<% content_for :top_content do %>

              <% if user_signed_in? %>

                <%= render "dashboard" %>

              <% else %>

                <%= render "splash" %>

              <% end %>

<% end %>
<h1>Listing articles</h1>
 
<table>
  <tr>
    <th>Title</th>
    <th>Text</th>
  </tr>
 
  <% @applications.each do |application| %>
    <tr>
      <td><%= application.confernece_name %></td>
      <td><%= article.status %></td>
      <td><%= link_to 'Show', application_path(application) %></td>
    </tr>
  <% end %>
</table>